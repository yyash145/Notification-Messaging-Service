enum Role {
  SUPER_ADMIN
  ADMIN
  USER
  CLIENT
}

enum PlanType {
  FREE
  PRO
  ENTERPRISE
}

enum MessageLogStatus {
  SENT
  FAILED
  PENDING
}

enum MessageStatus {
  QUEUED
  SENT
  FAILED
}

enum TemplateCategory {
  GLOBAL
  USER
}
